namespace breaking_worse.Input.Enums;

public enum MouseButtons
// this enum is named in plural on purpose, as the monogame key enums are also in plural
{
    /// <summary>
    /// enum of the buttons the mouse has
    /// </summary>
    
    LeftButton,
    MiddleButton,
    RightButton,
    XButton1,
    XButton2
}
