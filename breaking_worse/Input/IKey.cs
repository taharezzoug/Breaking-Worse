namespace breaking_worse.Input;

public interface IKey
{
    /// <summary>
    /// interface to group different kind of keys, like keyboard and gamepad
    /// </summary>
    
    bool isPressed();
}
